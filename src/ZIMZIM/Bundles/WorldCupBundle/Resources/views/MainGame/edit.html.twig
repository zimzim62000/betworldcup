{% extends '::base.html.twig' %}


{% block title %}
    {{ 'views.worldcup.maingame.edit.title'|trans }}
{% endblock title %}

{% block page %}
    <div class="row">
        <div class="small-12 large-12 columns">
            <h1 class="text-center title">
                {{ 'views.worldcup.maingame.edit.title'|trans }}
            </h1>
        </div>
    </div>
    <div class="row">
        <div class="small-12 large-12 columns">
            {{ form(edit_form) }}
        </div>
    </div>

    <div class="row">
        <div class="small-12 large-4 columns text-center">
            <a class="tiny button" href="{{ path('zimzim_worldcup_maingame') }}">
                {{ 'link.return'|trans }}
            </a>
        </div>
        <div class="small-12 large-4 columns text-center">
            {{ form(delete_form) }}
        </div>
    </div>

{% endblock %}
